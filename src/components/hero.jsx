import React from 'react';
import './hero.css'

const HeroSection = () => {
    return (
        <section className="hero">
            <div className="hero-content">
            </div>
        </section>
    );
};

export default HeroSection;